# ========================================
# MY TIME FRONTEND — ENVIRONMENT VARIABLES
# ========================================

# Backend API URL (required for deployment on separate server)
# In development, defaults to http://localhost:5001/api if not set
VITE_API_URL=http://localhost:5001/api
